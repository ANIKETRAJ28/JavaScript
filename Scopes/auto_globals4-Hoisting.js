gun();
function gun() {
  console.log("shooot");
}
/**
 * Hosting is a direct consequence of lexical parsing that happens in JS, due to which we are able to access some function and variables before declaring them.
 * Which gives us a notion that they are moved up in the file.
 */
